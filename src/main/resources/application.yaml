spring:
  config:
    name: project2
  datasource:
    url: jdbc:postgresql://localhost:5432/sclad
    username: postgres
    password: 1212
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl_auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql:
